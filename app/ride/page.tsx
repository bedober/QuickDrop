'use client';
import { useState } from 'react';
import { Bike, Car, Map, MapPin, Navigation, ShieldCheck } from 'lucide-react';
import Link from 'next/link';
const vehicles=[{type:'Boda Boda',emoji:'🏍️',fare:6500,icon:Bike},{type:'TukTuk',emoji:'🛺',fare:8500,icon:Navigation},{type:'Car',emoji:'🚗',fare:12000,icon:Car}];
export default function RidePage(){const [vehicle,setVehicle]=useState(vehicles[0]);const [booked,setBooked]=useState(false);if(booked)return <main className="mx-auto max-w-xl px-5 pb-16"><div className="card p-8 text-center"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-lime text-leaf"><ShieldCheck/></div><h1 className="mt-5 text-3xl font-black">Ride request sent</h1><p className="mt-2 text-slate-500">We&apos;re finding a {vehicle.type.toLowerCase()} driver for you.</p><Link className="btn-primary mt-6 inline-block" href="/tracking/QD-R6312">Track ride</Link></div></main>;return <main className="mx-auto max-w-5xl px-5 pb-16"><div className="mb-8"><Link href="/" className="text-sm font-bold text-slate-500">← Back home</Link><h1 className="mt-5 text-4xl font-black">Book a ride</h1><p className="mt-2 text-slate-500">Safe, reliable rides around Kampala.</p></div><div className="grid gap-6 lg:grid-cols-[1fr_.8fr]"><div className="card p-6"><label className="block text-sm font-bold">Pickup<input className="field mt-2" placeholder="Where are you? 📍" required/></label><label className="mt-4 block text-sm font-bold">Dropoff<input className="field mt-2" placeholder="Where to? 📍" required/></label><div className="mt-5 grid min-h-[230px] place-items-center rounded-3xl bg-[#dcead8]" style={{backgroundImage:'linear-gradient(35deg,transparent 45%,#fff 46%,#fff 48%,transparent 49%),linear-gradient(125deg,transparent 45%,#fff 46%,#fff 48%,transparent 49%)',backgroundSize:'75px 75px'}}><div className="rounded-2xl bg-white/90 p-4 text-center"><Map className="mx-auto text-leaf"/><p className="mt-1 text-sm font-bold">Route preview</p><p className="text-xs text-slate-500">4.2 km · Kampala</p></div></div></div><div className="rounded-3xl bg-[#e9f2ef] p-6"><p className="text-sm font-bold text-leaf">CHOOSE YOUR RIDE</p><div className="mt-4 space-y-2">{vehicles.map(item=><button key={item.type} onClick={()=>setVehicle(item)} className={`flex w-full items-center gap-3 rounded-2xl border p-3 text-left ${vehicle.type===item.type?'border-leaf bg-lime/30':'border-black/10 bg-white'}`}><span className="text-2xl">{item.emoji}</span><span className="flex-1"><b>{item.type}</b><small className="block text-slate-500">12–18 min</small></span><b>UGX {item.fare.toLocaleString()}</b></button>)}</div><div className="mt-7 border-t border-black/10 pt-5"><p className="text-sm text-slate-500">Estimated fare</p><p className="text-4xl font-black">UGX {vehicle.fare.toLocaleString()}</p><button onClick={()=>setBooked(true)} className="btn-primary mt-5 w-full">Book {vehicle.type}</button></div></div></div></main>}
