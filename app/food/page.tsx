import { Search, Star } from 'lucide-react';
import { restaurants } from '@/app/lib/mock-data';
export default function Food(){return <main className="mx-auto max-w-7xl px-5 pb-16"><div className="mb-8"><p className="text-sm font-bold text-leaf">FOOD DELIVERY</p><h1 className="mt-1 text-4xl font-black">Restaurants near you</h1><label className="mt-5 flex max-w-md items-center gap-2 rounded-2xl bg-white px-4 py-3 shadow-sm"><Search size={18} className="text-slate-400"/><input className="w-full bg-transparent outline-none" placeholder="Search restaurants or meals"/></label></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{restaurants.map((r)=><article className="card p-4" key={r.id}><div className={`grid h-40 place-items-center rounded-3xl ${r.accent} text-6xl`}>{r.emoji}</div><div className="mt-4 flex justify-between"><h2 className="font-black">{r.name}</h2><span className="flex items-center gap-1 text-sm"><Star size={14} className="fill-orange-400 text-orange-400"/>{r.rating}</span></div><p className="mt-1 text-sm text-slate-500">{r.cuisine} · {r.eta}</p></article>)}</div></main>}
